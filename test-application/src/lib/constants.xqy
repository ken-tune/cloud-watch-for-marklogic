xquery version "1.0-ml";

module namespace constants="http://marklogic.com/cloud-watch-demo/constants";

declare variable $DOCS-DIRECTORY := "/docs/";

declare variable $MIN-DOCUMENTS-RETURNED := 1;
declare variable $MAX-DOCUMENTS-RETURNED := 10;

declare variable $MIN-DOCUMENTS-INSERTED := 1;
declare variable $MAX-DOCUMENTS-INSERTED := 10;

